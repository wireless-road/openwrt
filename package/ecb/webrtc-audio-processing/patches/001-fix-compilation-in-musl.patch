Index: webrtc-audio-processing-0.3.1/webrtc/base/checks.cc
===================================================================
--- webrtc-audio-processing-0.3.1.orig/webrtc/base/checks.cc
+++ webrtc-audio-processing-0.3.1/webrtc/base/checks.cc
@@ -17,8 +17,8 @@
 #include <cstdlib>
 
 #if defined(__GLIBCXX__) && !defined(__UCLIBC__)
-#include <cxxabi.h>
-#include <execinfo.h>
+//#include <cxxabi.h>
+//#include <execinfo.h>
 #endif
 
 #if defined(WEBRTC_ANDROID)
@@ -56,6 +56,7 @@ void PrintError(const char* format, ...)
 // advanced stace trace system; also more difficult to copy.
 void DumpBacktrace() {
 #if defined(__GLIBCXX__) && !defined(__UCLIBC__)
+/*
   void* trace[100];
   int size = backtrace(trace, sizeof(trace) / sizeof(*trace));
   char** symbols = backtrace_symbols(trace, size);
@@ -81,6 +82,7 @@ void DumpBacktrace() {
     }
   }
   free(symbols);
+*/
 #endif
 }
 
