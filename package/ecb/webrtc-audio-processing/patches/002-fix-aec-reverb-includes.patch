Index: webrtc-audio-processing-1.0/webrtc/modules/audio_processing/aec3/reverb_model_estimator.h
===================================================================
--- webrtc-audio-processing-1.0.orig/webrtc/modules/audio_processing/aec3/reverb_model_estimator.h
+++ webrtc-audio-processing-1.0/webrtc/modules/audio_processing/aec3/reverb_model_estimator.h
@@ -13,6 +13,7 @@
 
 #include <array>
 #include <vector>
+#include <memory>
 
 #include "absl/types/optional.h"
 #include "api/array_view.h"
@@ -35,11 +36,14 @@ class ReverbModelEstimator {
   // Updates the estimates based on new data.
   void Update(
       rtc::ArrayView<const std::vector<float>> impulse_responses,
+#include <memory>
       rtc::ArrayView<const std::vector<std::array<float, kFftLengthBy2Plus1>>>
+#include <memory>
           frequency_responses,
       rtc::ArrayView<const absl::optional<float>> linear_filter_qualities,
       rtc::ArrayView<const int> filter_delays_blocks,
       const std::vector<bool>& usable_linear_estimates,
+#include <memory>
       bool stationary_block);
 
   // Returns the exponential decay of the reverberant echo.
@@ -59,7 +63,9 @@ class ReverbModelEstimator {
 
  private:
   std::vector<std::unique_ptr<ReverbDecayEstimator>> reverb_decay_estimators_;
+#include <memory>
   std::vector<ReverbFrequencyResponse> reverb_frequency_responses_;
+#include <memory>
 };
 
 }  // namespace webrtc
